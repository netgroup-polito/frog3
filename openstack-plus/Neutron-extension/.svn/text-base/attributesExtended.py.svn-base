#created by Matteo
#as in attributes.py, new resources are defined with their own validators

from neutron.common import constants
from neutron.common import exceptions
FLOWRULE = 'flowrule'
FLOWRULES = '%ss' % FLOWRULE

"""class FlowruleNotFound(NotFound):
    message = _("Port %(port_id)s could not be found")


class FlowruleMalformed(NotFound):
    message = _("Port %(port_id)s could not be found")
"""